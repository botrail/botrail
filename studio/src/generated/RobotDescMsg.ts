// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { GroupMsg } from "./GroupMsg";
import type { JointMsg } from "./JointMsg";
import type { LinkMsg } from "./LinkMsg";
import type { PoseMsg } from "./PoseMsg";
import type { UsdAssetMsg } from "./UsdAssetMsg";

/**
 * One robot instance. The instance `name` is the scope key every
 * robot-addressed message uses from here on.
 */
export type RobotDescMsg = { 
/**
 * Scene-unique instance name.
 */
name: string, 
/**
 * When present, the client renders the robot from this USD asset
 * (client-side FK); the `links` visuals then serve only as fallback.
 */
usd_asset: UsdAssetMsg | null, 
/**
 * World pose of the robot's root link.
 */
base_pose: PoseMsg, links: Array<LinkMsg>, 
/**
 * `q_index` is the robot-local DOF index.
 */
joints: Array<JointMsg>, 
/**
 * Suggested end-effector link for the TCP gizmo (deepest leaf link).
 */
tcp_link: string | null, 
/**
 * The planning groups — the arms. One for a single-arm robot (its
 * whole DOF, tipped at `tcp_link`); one per arm for a dual-arm robot,
 * each with its own tip. What `group` on a client message names.
 */
groups: Array<GroupMsg>, };
